import {
  UPLOAD_TRAY_MIN_HEIGHT_PX,
  UPLOAD_TRAY_MIN_WIDTH_PX,
} from "./uploadTrayGeometry";

export const INFO_PANEL_GEOMETRY_STORAGE_KEY = "zfiles-info-panel-geometry";
export const INFO_PANEL_WIDTH_PX = 480;
export const INFO_PANEL_HEIGHT_PX = 560;

export const INFO_PANEL_MIN_WIDTH_PX = UPLOAD_TRAY_MIN_WIDTH_PX;
export const INFO_PANEL_MIN_HEIGHT_PX = UPLOAD_TRAY_MIN_HEIGHT_PX;

export const INFO_PANEL_SIZE_LIMITS = {
  minWidth: INFO_PANEL_MIN_WIDTH_PX,
  minHeight: INFO_PANEL_MIN_HEIGHT_PX,
};
